---
phase: 06-ai-upscaling-style-transfer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/ai-image.ts
  - src/components/editor/hooks/use-editor-store.ts
  - src/components/editor/hooks/use-upscale.ts
  - src/components/editor/hooks/use-style-transfer.ts
  - src/components/editor/upscale-panel.tsx
  - src/components/editor/style-transfer-panel.tsx
  - src/components/editor/tool-sidebar.tsx
  - src/components/editor/properties-panel.tsx
autonomous: true
requirements: [UPSC-01, UPSC-02, STYL-01, STYL-02]
---

<objective>
Add AI upscaling (2x/4x) and style transfer (5 preset styles with intensity slider) to the image editor.

Purpose: Complete the AI feature set by adding resolution enhancement and artistic style conversion, using fal.ai models via existing AI SDK infrastructure.

Output: Two new server actions, two hooks, two panel components, extended store and sidebar/properties wiring.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add upscale and styleTransfer server actions, extend editor store and sidebar</name>
  <files>src/app/actions/ai-image.ts, src/components/editor/hooks/use-editor-store.ts, src/components/editor/tool-sidebar.tsx, src/components/editor/properties-panel.tsx</files>
  <action>
1. Add two new server actions to src/app/actions/ai-image.ts:
   - upscaleImage(base64Image: string, scale: 2 | 4): Promise<{ cdnUrl: string }> - calls fal.image("fal-ai/creative-upscaler") with scale parameter, uploads result to S3
   - styleTransfer(base64Image: string, style: string, intensity: number): Promise<{ cdnUrl: string }> - calls fal.image("fal-ai/flux/dev") with style prompt, uploads result to S3

2. Extend ActiveTool type in use-editor-store.ts with "upscale" | "style-transfer"

3. Add upscale and style-transfer tools to tool-sidebar.tsx AI_TOOLS array

4. Add upscale and style-transfer panel rendering to properties-panel.tsx
  </action>
  <done>Server actions compile, store extended, sidebar shows new tools, properties panel routes to new panels</done>
</task>

<task type="auto">
  <name>Task 2: Create upscale hook + panel and style transfer hook + panel</name>
  <files>src/components/editor/hooks/use-upscale.ts, src/components/editor/hooks/use-style-transfer.ts, src/components/editor/upscale-panel.tsx, src/components/editor/style-transfer-panel.tsx</files>
  <action>
1. Create use-upscale.ts hook following use-bg-removal.ts pattern - exports handleUpscale(scale: 2|4) that calls upscaleImage server action

2. Create use-style-transfer.ts hook - exports handleStyleTransfer(style, intensity) that calls styleTransfer server action

3. Create UpscalePanel component with 2x/4x buttons

4. Create StyleTransferPanel with preset style buttons and intensity slider
  </action>
  <done>Both panels render, hooks call server actions, all TypeScript compiles</done>
</task>

</tasks>

<verification>
- pnpm exec tsc --noEmit passes
- All new files exist and export correctly
</verification>

<success_criteria>
- upscaleImage and styleTransfer server actions exist
- Upscale panel shows 2x/4x options
- Style transfer panel shows 5 preset styles with intensity slider
- New tools appear in sidebar
- All TypeScript compiles
</success_criteria>

<output>
After completion, create .planning/phases/06-ai-upscaling-style-transfer/06-01-SUMMARY.md
</output>
