---
phase: 02-core-editor
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/editor/crop-overlay.tsx
  - src/components/editor/lib/crop-utils.ts
  - src/components/editor/sns-presets.tsx
  - src/components/editor/properties-panel.tsx
  - src/components/editor/hooks/use-fabric.ts
  - src/components/editor/lib/crop-utils.test.ts
  - src/components/editor/lib/presets.test.ts
autonomous: true
requirements:
  - EDIT-03
  - UI-02

must_haves:
  truths:
    - "User can activate crop tool and see a draggable crop region with dark mask overlay"
    - "User can select crop ratio presets: Free, 1:1, 4:5, 9:16, 16:9, 1.91:1"
    - "User can select SNS platform presets: IG Story, IG Post, FB Post, YT Thumbnail, TikTok, Twitter/X"
    - "User can drag corner handles to resize the crop region"
    - "User can apply crop and the canvas updates to show only the cropped area"
    - "Crop is reversible via undo"
  artifacts:
    - path: "src/components/editor/crop-overlay.tsx"
      provides: "Crop region with dark mask and corner handles"
      contains: "cropRect"
    - path: "src/components/editor/lib/crop-utils.ts"
      provides: "Crop math: coordinate conversion, aspect ratio constraint"
      exports: ["applyCrop", "constrainToAspectRatio"]
    - path: "src/components/editor/sns-presets.tsx"
      provides: "SNS preset selector UI component"
      contains: "SNS_PRESETS"
    - path: "src/components/editor/lib/crop-utils.test.ts"
      provides: "Unit tests for crop math"
      contains: "constrainToAspectRatio"
    - path: "src/components/editor/lib/presets.test.ts"
      provides: "Unit tests for preset data integrity"
      contains: "SNS_PRESETS"
  key_links:
    - from: "src/components/editor/crop-overlay.tsx"
      to: "src/components/editor/lib/crop-utils.ts"
      via: "import crop math functions"
      pattern: "import.*crop-utils"
    - from: "src/components/editor/properties-panel.tsx"
      to: "src/components/editor/sns-presets.tsx"
      via: "renders SNS preset selector when crop tool active"
      pattern: "SnsPresetSelector"
    - from: "src/components/editor/crop-overlay.tsx"
      to: "src/components/editor/hooks/use-editor-store.ts"
      via: "pushes new canvasJson after crop apply"
      pattern: "setCanvasJson"
---

<objective>
Implement the crop tool with visual overlay (dark mask + draggable handles), aspect ratio presets, and SNS platform presets. Crop operations update the canvas state and are reversible via undo.

Purpose: Crop is the primary non-AI editing tool. SNS presets (UI-02) make the editor immediately useful for social media content creators -- the target audience.
Output: Working crop tool with 6 ratio presets and 6 SNS platform presets, unit-tested crop math.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-editor/02-CONTEXT.md
@.planning/phases/02-core-editor/02-RESEARCH.md
@.planning/phases/02-core-editor/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/editor/hooks/use-editor-store.ts:
```typescript
interface EditorState {
  canvasJson: string | null;
  activeTool: "select" | "crop" | "resize" | "pan";
  zoom: number;
  isCropping: boolean;
  selectedPreset: string | null;
  setCanvasJson: (json: string) => void;
  setActiveTool: (tool: EditorState["activeTool"]) => void;
  setIsCropping: (v: boolean) => void;
  setSelectedPreset: (preset: string | null) => void;
}
export const useEditorStore = create<EditorState>()(temporal(...));
```

From src/components/editor/hooks/use-fabric.ts:
```typescript
export function useFabric(canvasRef: RefObject<HTMLCanvasElement>, imageUrl: string): {
  fabricRef: RefObject<FabricCanvas | null>;
  isLoading: boolean;
}
```

From src/components/editor/lib/presets.ts:
```typescript
export interface SnsPreset { name: string; width: number; height: number; platform: string; }
export const SNS_PRESETS: SnsPreset[];
export const CROP_RATIOS: { name: string; ratio: number | null }[];
```

From src/components/editor/properties-panel.tsx:
```typescript
// Shows context-sensitive controls based on activeTool
// When activeTool is "crop": renders crop-specific controls (this plan fills that in)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crop math utilities and SNS preset tests</name>
  <files>
    src/components/editor/lib/crop-utils.ts,
    src/components/editor/lib/crop-utils.test.ts,
    src/components/editor/lib/presets.test.ts
  </files>
  <read_first>
    src/components/editor/lib/presets.ts,
    src/components/editor/hooks/use-editor-store.ts,
    vitest.config.ts
  </read_first>
  <behavior>
    crop-utils.test.ts:
    - constrainToAspectRatio(rect, 1): returns square with same area center
    - constrainToAspectRatio(rect, 16/9): returns 16:9 rect within bounds
    - constrainToAspectRatio(rect, null): returns original rect unchanged (free crop)
    - clampCropRegion(region, canvasBounds): clamps region within canvas boundaries
    - getCropPixelCoords(cropRect, imageObj): converts viewport coords to image pixel coords accounting for image scale

    presets.test.ts:
    - SNS_PRESETS has exactly 6 entries
    - Each preset has name, width, height, platform fields
    - Instagram Story is 1080x1920
    - CROP_RATIOS has exactly 6 entries
    - CROP_RATIOS includes Free with null ratio
    - All non-null ratios are positive numbers
  </behavior>
  <action>
    1. Create `src/components/editor/lib/crop-utils.ts`:
       - `constrainToAspectRatio(rect: { x: number; y: number; width: number; height: number }, ratio: number | null): { x, y, width, height }` -- if ratio is null, return rect unchanged. Otherwise, adjust width or height to match ratio, keeping center position.
       - `clampCropRegion(region: { x, y, width, height }, bounds: { width, height }): { x, y, width, height }` -- ensure crop region stays within canvas bounds
       - `getCropPixelCoords(cropRect: { left, top, width, height, scaleX, scaleY }, imageObj: { left, top, scaleX, scaleY }): { x, y, width, height }` -- convert from viewport/canvas coordinates to actual image pixel coordinates, accounting for image and cropRect scale transforms

    2. Create tests first (RED), then implement to pass (GREEN):
       - `src/components/editor/lib/crop-utils.test.ts` with the behaviors above
       - `src/components/editor/lib/presets.test.ts` with the preset validation behaviors above

    3. Ensure vitest config supports these tests. If vitest.config.ts has `environment: "node"`, that is fine for pure math/data tests.
  </action>
  <verify>
    <automated>cd /Users/iron/Project/image-editor && npx vitest run src/components/editor/lib/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "constrainToAspectRatio" src/components/editor/lib/crop-utils.ts` shows function exists
    - `grep -r "clampCropRegion" src/components/editor/lib/crop-utils.ts` shows function exists
    - `grep -r "getCropPixelCoords" src/components/editor/lib/crop-utils.ts` shows function exists
    - All tests in crop-utils.test.ts pass
    - All tests in presets.test.ts pass
    - SNS_PRESETS validated: 6 entries with correct dimensions
  </acceptance_criteria>
  <done>
    Crop math utility functions exist with full test coverage. Preset data integrity verified by tests. All vitest tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crop overlay component, SNS preset selector, and properties panel integration</name>
  <files>
    src/components/editor/crop-overlay.tsx,
    src/components/editor/sns-presets.tsx,
    src/components/editor/properties-panel.tsx,
    src/components/editor/hooks/use-fabric.ts,
    src/components/editor/canvas.tsx
  </files>
  <read_first>
    src/components/editor/lib/crop-utils.ts,
    src/components/editor/lib/presets.ts,
    src/components/editor/hooks/use-editor-store.ts,
    src/components/editor/hooks/use-fabric.ts,
    src/components/editor/canvas.tsx,
    src/components/editor/properties-panel.tsx,
    src/components/editor/editor-shell.tsx,
    .planning/phases/02-core-editor/02-RESEARCH.md
  </read_first>
  <action>
    1. Create `src/components/editor/crop-overlay.tsx` ("use client") per D-10:
       - Receives `fabricRef: RefObject<FabricCanvas | null>` prop
       - When crop mode activates (useEditorStore activeTool === "crop"):
         a. Create a Fabric.js Rect for the crop region (white border, slight fill)
         b. Create 4 semi-transparent dark Rects for the mask around the crop region
         c. Crop Rect has corner controls enabled for resizing (uses Fabric.js built-in controls)
         d. Crop Rect moves and resizes freely when selectedPreset ratio is null (Free)
         e. When a ratio preset is selected, constrain crop region via `constrainToAspectRatio` from crop-utils
       - "Apply Crop" function per D-13:
         a. Get crop pixel coordinates via `getCropPixelCoords`
         b. Use `canvas.toDataURL()` to export the cropped region
         c. Clear canvas, load cropped image as new FabricImage
         d. Update canvas dimensions to match cropped size
         e. Push new canvas JSON to store via `setCanvasJson` (makes it undoable)
         f. Remove crop overlay objects
         g. Set activeTool back to "select"
       - "Cancel Crop" function: remove all crop overlay objects, set activeTool to "select"
       - Export `CropOverlay` component and `useCropActions` hook (for apply/cancel from properties panel)

    2. Create `src/components/editor/sns-presets.tsx` ("use client") per D-23, D-24:
       - Renders a grid/list of SNS preset buttons
       - Each button shows: platform icon (or emoji), preset name, dimensions text
       - On click: set crop region to match preset dimensions (aspect ratio), set selectedPreset in store
       - Also usable as "resize canvas to preset" option (sets canvas dimensions directly)
       - Import SNS_PRESETS and CROP_RATIOS from presets.ts

    3. Update `src/components/editor/properties-panel.tsx`:
       - When activeTool === "crop":
         a. Show CROP_RATIOS as selectable buttons (radio-style, one active at a time) per D-11
         b. Show SNS preset selector (SnsPresetSelector component) per D-23
         c. Show "Apply Crop" and "Cancel" buttons at bottom
       - Keep existing placeholder content for other tools

    4. Update `src/components/editor/canvas.tsx`:
       - Integrate CropOverlay component, pass fabricRef
       - CropOverlay renders only when activeTool === "crop"

    5. Update `src/components/editor/hooks/use-fabric.ts` if needed:
       - Ensure canvas events (`object:modified`) properly fire when crop rect is manipulated
       - Do NOT push crop overlay objects to undo stack (they are UI-only)
  </action>
  <verify>
    <automated>cd /Users/iron/Project/image-editor && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "cropRect\|CropOverlay" src/components/editor/crop-overlay.tsx` shows crop overlay component
    - `grep -r "SNS_PRESETS" src/components/editor/sns-presets.tsx` shows preset selector
    - `grep -r "constrainToAspectRatio" src/components/editor/crop-overlay.tsx` shows ratio constraint usage
    - `grep -r "getCropPixelCoords" src/components/editor/crop-overlay.tsx` shows coordinate conversion
    - `grep -r "setCanvasJson" src/components/editor/crop-overlay.tsx` shows state push after crop apply
    - `grep -r "CropOverlay\|SnsPresetSelector" src/components/editor/properties-panel.tsx` shows integration
    - `grep -r "Apply Crop\|Cancel" src/components/editor/properties-panel.tsx` shows action buttons
    - Next.js build succeeds
  </acceptance_criteria>
  <done>
    Crop tool works: activating shows dark mask overlay with draggable crop region. Ratio presets (Free, 1:1, 4:5, 9:16, 16:9, 1.91:1) constrain the crop region. SNS presets (6 platforms) set crop to platform dimensions. Apply crop updates canvas destructively (reversible via undo). Cancel removes overlay. Build passes.
  </done>
</task>

</tasks>

<verification>
- Crop tool activates from sidebar, shows dark mask overlay with draggable region
- Ratio presets constrain crop aspect ratio correctly
- SNS presets set appropriate dimensions
- Apply crop produces correct cropped image on canvas
- Undo after crop restores previous state
- All unit tests pass: `npx vitest run src/components/editor/lib/`
- Build passes: `npx next build`
</verification>

<success_criteria>
- Crop overlay with dark mask and corner handles renders when crop tool active
- 6 crop ratio presets work correctly (Free, 1:1, 4:5, 9:16, 16:9, 1.91:1)
- 6 SNS platform presets selectable (IG Story/Post, FB Post, YT Thumbnail, TikTok, Twitter/X)
- Crop apply updates canvas, reversible via undo
- Crop math has unit tests passing
- Preset data integrity has unit tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-editor/02-02-SUMMARY.md`
</output>
