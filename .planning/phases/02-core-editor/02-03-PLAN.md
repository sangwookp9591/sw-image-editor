---
phase: 02-core-editor
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/editor/resize-controls.tsx
  - src/components/editor/export-modal.tsx
  - src/components/editor/lib/export-utils.ts
  - src/components/editor/lib/resize-utils.ts
  - src/components/editor/properties-panel.tsx
  - src/components/editor/toolbar.tsx
  - src/components/editor/lib/export-utils.test.ts
  - src/components/editor/lib/resize-utils.test.ts
autonomous: true
requirements:
  - EDIT-04
  - EDIT-06

must_haves:
  truths:
    - "User can specify pixel dimensions (width and height) to resize the image"
    - "User can toggle aspect ratio lock when resizing"
    - "User can open export modal and select format: PNG, JPG, or WebP"
    - "User can adjust quality for JPG/WebP with a slider (1-100, default 90)"
    - "User can select resolution multiplier: Original (1x), 2x, 0.5x, Custom"
    - "User can download the edited image via browser file save dialog"
    - "Resize is reversible via undo"
  artifacts:
    - path: "src/components/editor/resize-controls.tsx"
      provides: "Width/height inputs with aspect ratio lock toggle"
      contains: "aspectLock"
    - path: "src/components/editor/export-modal.tsx"
      provides: "Export dialog with format, quality, and resolution options"
      contains: "Dialog"
    - path: "src/components/editor/lib/export-utils.ts"
      provides: "Canvas export and download helpers"
      exports: ["exportCanvas", "downloadDataUrl"]
    - path: "src/components/editor/lib/resize-utils.ts"
      provides: "Resize calculation with aspect ratio lock"
      exports: ["calculateResize"]
    - path: "src/components/editor/lib/export-utils.test.ts"
      provides: "Unit tests for export config generation"
      contains: "exportConfig"
    - path: "src/components/editor/lib/resize-utils.test.ts"
      provides: "Unit tests for resize calculations"
      contains: "calculateResize"
  key_links:
    - from: "src/components/editor/export-modal.tsx"
      to: "src/components/editor/lib/export-utils.ts"
      via: "calls exportCanvas with user-selected options"
      pattern: "exportCanvas"
    - from: "src/components/editor/resize-controls.tsx"
      to: "src/components/editor/lib/resize-utils.ts"
      via: "calls calculateResize for aspect ratio lock"
      pattern: "calculateResize"
    - from: "src/components/editor/toolbar.tsx"
      to: "src/components/editor/export-modal.tsx"
      via: "export button opens modal"
      pattern: "ExportModal"
---

<objective>
Implement the resize tool (width/height with aspect ratio lock) and export/download modal (PNG/JPG/WebP with quality slider and resolution options). Both features integrate into the existing editor layout.

Purpose: Resize and export complete the core non-AI editing workflow. Users can load, crop, resize, and download -- a fully functional image editor.
Output: Working resize tool in properties panel, export modal with format/quality/resolution selection, browser download.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-editor/02-CONTEXT.md
@.planning/phases/02-core-editor/02-RESEARCH.md
@.planning/phases/02-core-editor/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/editor/hooks/use-editor-store.ts:
```typescript
interface EditorState {
  canvasJson: string | null;
  activeTool: "select" | "crop" | "resize" | "pan";
  zoom: number;
  setCanvasJson: (json: string) => void;
  setActiveTool: (tool: EditorState["activeTool"]) => void;
}
export const useEditorStore = create<EditorState>()(temporal(...));
```

From src/components/editor/hooks/use-fabric.ts:
```typescript
export function useFabric(...): { fabricRef: RefObject<FabricCanvas | null>; isLoading: boolean; }
```

From src/components/editor/toolbar.tsx:
```typescript
// Has export button placeholder that needs to open ExportModal
```

From src/components/editor/properties-panel.tsx:
```typescript
// Shows context-sensitive controls based on activeTool
// When activeTool is "resize": needs to render ResizeControls (this plan fills that in)
```

Existing shadcn/ui components (installed in Plan 01): dialog, slider, tabs, tooltip, toggle, select, button, input, label
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resize and export utility functions with tests</name>
  <files>
    src/components/editor/lib/resize-utils.ts,
    src/components/editor/lib/resize-utils.test.ts,
    src/components/editor/lib/export-utils.ts,
    src/components/editor/lib/export-utils.test.ts
  </files>
  <read_first>
    src/components/editor/hooks/use-editor-store.ts,
    src/components/editor/lib/presets.ts,
    vitest.config.ts
  </read_first>
  <behavior>
    resize-utils.test.ts:
    - calculateResize({ width: 1920, height: 1080 }, "width", 960, true) returns { width: 960, height: 540 } (locked aspect ratio, changing width)
    - calculateResize({ width: 1920, height: 1080 }, "height", 540, true) returns { width: 960, height: 540 } (locked, changing height)
    - calculateResize({ width: 1920, height: 1080 }, "width", 500, false) returns { width: 500, height: 1080 } (unlocked, only width changes)
    - calculateResize({ width: 100, height: 100 }, "width", 0, true) returns { width: 1, height: 1 } (minimum 1px)
    - calculateResize({ width: 1920, height: 1080 }, "width", -100, true) returns { width: 1, height: 1 } (negative clamped to 1)

    export-utils.test.ts:
    - buildExportConfig("png", 90, 1) returns { format: "png", quality: undefined, multiplier: 1 } (PNG ignores quality)
    - buildExportConfig("jpeg", 85, 2) returns { format: "jpeg", quality: 0.85, multiplier: 2 } (quality 0-1 scale)
    - buildExportConfig("webp", 50, 0.5) returns { format: "webp", quality: 0.5, multiplier: 0.5 }
    - getFileExtension("png") returns "png"
    - getFileExtension("jpeg") returns "jpg"
    - getFileExtension("webp") returns "webp"
    - getFileName("my-photo", "jpeg") returns "my-photo.jpg"
  </behavior>
  <action>
    1. Create `src/components/editor/lib/resize-utils.ts`:
       - `calculateResize(current: { width: number; height: number }, changed: "width" | "height", newValue: number, locked: boolean): { width: number; height: number }` per D-12
         - If locked: calculate the other dimension from aspect ratio of `current`
         - Clamp minimum to 1px for both dimensions
         - Return integer pixel values (Math.round)

    2. Create `src/components/editor/lib/export-utils.ts`:
       - `buildExportConfig(format: "png" | "jpeg" | "webp", quality: number, multiplier: number): { format: string; quality?: number; multiplier: number }` per D-18, D-19, D-20
         - For PNG, quality is undefined (PNG is lossless)
         - For JPEG/WebP, convert quality from 1-100 to 0-1 scale (Fabric.js expects 0-1)
       - `getFileExtension(format: "png" | "jpeg" | "webp"): string` -- returns "jpg" for "jpeg", otherwise format name
       - `getFileName(baseName: string, format: "png" | "jpeg" | "webp"): string` -- combines name + extension
       - `downloadDataUrl(dataUrl: string, fileName: string): void` per D-21 -- creates anchor element, sets href to dataUrl, sets download to fileName, clicks, removes anchor

    3. Write tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/iron/Project/image-editor && npx vitest run src/components/editor/lib/resize-utils.test.ts src/components/editor/lib/export-utils.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "calculateResize" src/components/editor/lib/resize-utils.ts` shows function exists
    - `grep -r "buildExportConfig" src/components/editor/lib/export-utils.ts` shows function exists
    - `grep -r "downloadDataUrl" src/components/editor/lib/export-utils.ts` shows download function
    - All resize-utils tests pass (aspect lock, clamp, unlocked)
    - All export-utils tests pass (config build, extension, filename)
  </acceptance_criteria>
  <done>
    Resize and export utility functions exist with full test coverage. calculateResize handles aspect ratio lock with clamping. buildExportConfig converts quality scale correctly. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resize controls, export modal, and integration into editor layout</name>
  <files>
    src/components/editor/resize-controls.tsx,
    src/components/editor/export-modal.tsx,
    src/components/editor/properties-panel.tsx,
    src/components/editor/toolbar.tsx
  </files>
  <read_first>
    src/components/editor/lib/resize-utils.ts,
    src/components/editor/lib/export-utils.ts,
    src/components/editor/hooks/use-editor-store.ts,
    src/components/editor/hooks/use-fabric.ts,
    src/components/editor/properties-panel.tsx,
    src/components/editor/toolbar.tsx,
    src/components/editor/editor-shell.tsx,
    src/components/editor/canvas.tsx
  </read_first>
  <action>
    1. Create `src/components/editor/resize-controls.tsx` ("use client") per D-12, D-13:
       - Receives `fabricRef: RefObject<FabricCanvas | null>` prop
       - Displays current image dimensions (width x height in px)
       - Two input fields (shadcn Input): Width and Height, type="number"
       - Aspect ratio lock toggle (shadcn Toggle with Lock/Unlock icon from lucide-react) per D-12
       - When user changes width/height:
         a. Call `calculateResize(current, changedField, newValue, isLocked)` from resize-utils
         b. Get the image object from canvas: `fabricRef.current.getObjects()[0]`
         c. Scale image to new dimensions: set `scaleX = newWidth / image.width`, `scaleY = newHeight / image.height`
         d. Update canvas dimensions: `canvas.setDimensions({ width: newWidth, height: newHeight })`
         e. Center image on canvas
         f. Push new canvas JSON to store via `setCanvasJson` (makes it undoable) per D-13
       - "Apply Resize" button to confirm, or apply on input blur
       - Show SNS presets as quick-resize options (import SNS_PRESETS from presets.ts)

    2. Create `src/components/editor/export-modal.tsx` ("use client") per D-18, D-19, D-20, D-21, D-22:
       - Uses shadcn Dialog component
       - Receives `fabricRef: RefObject<FabricCanvas | null>`, `imageName: string`, `open: boolean`, `onOpenChange: (open: boolean) => void` props
       - Format selection: radio buttons or shadcn Select for PNG, JPG, WebP per D-18
       - Quality slider: shadcn Slider, range 1-100, default 90 per D-19
         - Only visible when format is JPG or WebP (PNG is lossless)
         - Show current value label next to slider
       - Resolution options: shadcn Select with Original (1x), 2x, 0.5x, Custom per D-20
         - When Custom selected, show input for custom multiplier value
       - Preview section: show estimated output dimensions (currentWidth * multiplier x currentHeight * multiplier)
       - Note about PNG transparency per D-22: "PNG preserves transparency" info text
       - "Download" button:
         a. Call `buildExportConfig(format, quality, multiplier)` from export-utils
         b. Call `fabricRef.current.toDataURL(config)` to get data URL
         c. Call `downloadDataUrl(dataUrl, getFileName(imageName, format))` to trigger download
         d. Show success toast via sonner
         e. Close modal
       - "Cancel" button closes modal

    3. Update `src/components/editor/properties-panel.tsx`:
       - When activeTool === "resize": render ResizeControls component, pass fabricRef
       - Keep crop controls from Plan 02 (or its placeholder if this runs first in Wave 2)

    4. Update `src/components/editor/toolbar.tsx`:
       - Wire export button to open ExportModal
       - Add ExportModal component with state: `const [exportOpen, setExportOpen] = useState(false)`
       - Pass fabricRef and imageName to ExportModal
  </action>
  <verify>
    <automated>cd /Users/iron/Project/image-editor && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "calculateResize" src/components/editor/resize-controls.tsx` shows resize math integration
    - `grep -r "aspectLock\|isLocked\|lockAspect" src/components/editor/resize-controls.tsx` shows aspect ratio lock
    - `grep -r "setCanvasJson" src/components/editor/resize-controls.tsx` shows undo support
    - `grep -r "Dialog" src/components/editor/export-modal.tsx` shows modal component
    - `grep -r "Slider" src/components/editor/export-modal.tsx` shows quality slider
    - `grep -r "toDataURL" src/components/editor/export-modal.tsx` shows canvas export
    - `grep -r "downloadDataUrl" src/components/editor/export-modal.tsx` shows download trigger
    - `grep -r "png\|jpeg\|webp" src/components/editor/export-modal.tsx` shows format options
    - `grep -r "ResizeControls" src/components/editor/properties-panel.tsx` shows integration
    - `grep -r "ExportModal" src/components/editor/toolbar.tsx` shows toolbar integration
    - Next.js build succeeds
  </acceptance_criteria>
  <done>
    Resize tool works: width/height inputs with aspect ratio lock toggle, resize updates canvas and is undoable. Export modal works: format selection (PNG/JPG/WebP), quality slider for JPG/WebP (1-100), resolution multiplier (1x/2x/0.5x/Custom), download triggers browser file save. Both integrated into editor layout. Build passes.
  </done>
</task>

</tasks>

<verification>
- Resize tool in properties panel with width/height inputs and aspect lock toggle
- Resize applies correctly and is reversible via undo
- Export modal opens from toolbar, shows format/quality/resolution options
- Quality slider only visible for JPG/WebP
- Download produces correct file format with expected quality
- All unit tests pass: `npx vitest run src/components/editor/lib/`
- Build passes: `npx next build`
</verification>

<success_criteria>
- Resize tool changes image dimensions with aspect ratio lock working correctly
- Export modal offers PNG, JPG, WebP formats
- Quality slider (1-100, default 90) works for JPG/WebP
- Resolution options: Original, 2x, 0.5x, Custom all produce correct output
- Download triggers browser file save with correct filename and format
- Resize and export utility functions have passing unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-editor/02-03-SUMMARY.md`
</output>
