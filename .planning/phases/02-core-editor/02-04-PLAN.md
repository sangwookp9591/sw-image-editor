---
phase: 02-core-editor
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EDIT-05
  - EDIT-06
  - UI-02

must_haves:
  truths:
    - "Complete editor workflow verified: load image, crop, resize, undo/redo, export"
    - "All 5 roadmap success criteria for Phase 2 are met"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 editor workflow end-to-end: image loading, crop with presets, resize with aspect lock, undo/redo, and export in multiple formats.

Purpose: Ensure all editor features work together as a cohesive experience before marking Phase 2 complete.
Output: Verified working editor or list of issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-editor/02-CONTEXT.md
@.planning/phases/02-core-editor/02-01-SUMMARY.md
@.planning/phases/02-core-editor/02-02-SUMMARY.md
@.planning/phases/02-core-editor/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of complete editor workflow</name>
  <files></files>
  <read_first>
    src/components/editor/editor-shell.tsx,
    src/components/editor/canvas.tsx,
    src/components/editor/crop-overlay.tsx,
    src/components/editor/resize-controls.tsx,
    src/components/editor/export-modal.tsx
  </read_first>
  <action>
    Run `npm run dev` and verify all 7 test scenarios below. This is a human-verify checkpoint.

    What was built:
    - Canvas rendering via Fabric.js at /editor/[imageId]
    - 3-panel layout: tool sidebar, canvas, properties panel
    - Crop tool with dark mask overlay, 6 ratio presets (Free, 1:1, 4:5, 9:16, 16:9, 1.91:1), 6 SNS presets (IG Story/Post, FB Post, YT Thumbnail, TikTok, Twitter/X)
    - Resize tool with width/height inputs and aspect ratio lock
    - Undo/redo (30 steps) via Ctrl+Z / Ctrl+Shift+Z and toolbar buttons
    - Export modal: PNG/JPG/WebP, quality slider, resolution multiplier (1x/2x/0.5x/Custom)
    - Clipboard paste to load images
    - Zoom (scroll wheel) and pan (space+drag)

    How to verify:

    **Test 1 - Image Loading (EDIT-01):**
    1. Log in and go to dashboard
    2. Upload an image via the dropzone
    3. Navigate to /editor/[imageId]
    4. Verify: image renders on canvas with dark background, 3-panel layout visible
    Expected: Image fills canvas, tool sidebar on left, properties panel on right

    **Test 2 - Clipboard Paste (EDIT-02):**
    1. Copy an image to clipboard (screenshot or copy from browser)
    2. Press Ctrl/Cmd+V in the editor
    3. Verify: pasted image appears on canvas
    Expected: Image loads onto canvas from clipboard

    **Test 3 - Crop Tool (EDIT-03 + UI-02):**
    1. Click Crop tool in left sidebar
    2. Verify: dark mask overlay appears with draggable crop region
    3. Select "1:1" ratio preset in properties panel
    4. Verify: crop region constrains to square
    5. Select "Instagram Story" SNS preset
    6. Verify: crop region constrains to 9:16 aspect ratio
    7. Drag corner handles to resize crop region
    8. Click "Apply Crop"
    9. Verify: canvas shows only the cropped area
    Expected: Crop works with all presets, apply produces correct result

    **Test 4 - Resize Tool (EDIT-04):**
    1. Click Resize tool in left sidebar
    2. Note current dimensions in properties panel
    3. Enable aspect ratio lock
    4. Change width to half the current value
    5. Verify: height updates proportionally
    6. Apply resize
    7. Verify: image is now smaller
    Expected: Resize with aspect lock works correctly

    **Test 5 - Undo/Redo (EDIT-05):**
    1. After crop and resize operations, press Ctrl+Z
    2. Verify: last operation is undone
    3. Press Ctrl+Z multiple times to undo several steps
    4. Press Ctrl+Shift+Z to redo
    5. Check toolbar undo/redo buttons work and disable when stack is empty
    Expected: At least 20 undo steps, keyboard and button both work

    **Test 6 - Export/Download (EDIT-06):**
    1. Click Export/Download button in toolbar
    2. Verify: modal opens with format, quality, resolution options
    3. Select JPG format, set quality to 80, resolution 2x
    4. Click Download -- verify file downloads with .jpg extension
    5. Repeat with PNG (verify quality slider hides)
    6. Repeat with WebP
    Expected: All 3 formats download correctly, quality slider only for JPG/WebP

    **Test 7 - Zoom/Pan:**
    1. Scroll mouse wheel over canvas -- verify zoom in/out centered on cursor
    2. Hold Space and drag -- verify canvas pans
    Expected: Smooth zoom and pan

    Resume signal: Type "approved" if all tests pass, or describe specific issues found.
  </action>
  <verify>
    <automated>cd /Users/iron/Project/image-editor && npx vitest run src/components/editor/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 7 manual test scenarios pass without critical issues
    - No console errors during normal editor usage
    - User approves the visual verification
  </acceptance_criteria>
  <done>
    All Phase 2 roadmap success criteria verified by human:
    1. User can drag-and-drop, click-to-browse, or paste from clipboard to load an image
    2. User can crop with free ratio and SNS platform presets
    3. User can resize by specifying pixel dimensions
    4. User can undo and redo at least 20 editing steps
    5. User can download as PNG, JPG, or WebP with quality and resolution options
  </done>
</task>

</tasks>

<verification>
All 5 roadmap success criteria for Phase 2 verified:
1. User can drag-and-drop, click-to-browse, or paste from clipboard to load an image onto the canvas
2. User can crop with free ratio and SNS platform presets
3. User can resize by specifying pixel dimensions
4. User can undo and redo at least 20 editing steps
5. User can download as PNG, JPG, or WebP with quality and resolution options
</verification>

<success_criteria>
- All 7 manual test scenarios pass without critical issues
- Editor is usable end-to-end: load -> edit -> download
- No console errors during normal usage
- User approves the visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-editor/02-04-SUMMARY.md`
</output>
